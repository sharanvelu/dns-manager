# docs-site is an independent npm project. The repo root .npmrc sets
# ignore-scripts=true for the Laravel app's toolchain; override here so
# Next.js/SWC install scripts run normally when building the docs site.
ignore-scripts=false
